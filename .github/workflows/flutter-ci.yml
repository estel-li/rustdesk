name: Full Flutter CI

on:
  # Manual only — go to the Actions tab and "Run workflow".
  # Auto-build on push/PR is intentionally disabled.
  workflow_dispatch:

jobs:
  run-ci:
    uses: ./.github/workflows/flutter-build.yml
    with:
      upload-artifact: false
